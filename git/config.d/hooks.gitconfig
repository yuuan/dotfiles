[hook "betterleaks"]
	event = pre-commit
	command = "command -v betterleaks >/dev/null 2>&1 || { echo \"betterleaks not installed; skipping secret scan\" >&2; exit 0; }; betterleaks git . --pre-commit --staged --redact"
