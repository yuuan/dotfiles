# ----------------------------------------
# エスケープ
# ----------------------------------------

# 文字列をシェルコマンドの引数に与えても安全なようにエスケープする
function sh-escape() {
	local s a=() q="'" qq='"'
	for s in "$@"; do
		a+=("'${s//$q/$q$qq$q$qq$q}'")
	done
	echo "${a[*]}"
}
