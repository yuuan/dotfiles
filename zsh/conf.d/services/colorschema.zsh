# ----------------------------------------
# 配色の設定
# ----------------------------------------

# `ls` コマンドにカラースキーマが設定されていたら補完にも使う
if [ -n "${LS_COLORS}" ]; then
    zstyle ':completion:*' list-colors ${(s.:.)LS_COLORS}
fi
